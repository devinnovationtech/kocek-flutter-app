// ignore_for_file: constant_identifier_names
part of '../class/kocek_environment.dart';

enum KocekFileSize { b, kb, mb }

enum KocekCRUD { create, read, update, delete }

enum KocekScope { internal, external }

enum KocekAPIType { get, post, put, delete, download }
